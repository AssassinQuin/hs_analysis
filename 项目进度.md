# RHEA 炉石传说分析引擎 — 项目进度

> 最后更新: 2026-04-23 | commit: `latest`

---

## ✅ 已完成任务

### 1. V11 RHEA 引擎核心
- **cross-turn planning** — 跨回合规划，决策考虑后续回合影响
- **adaptive time budget** — 自适应时间预算 (3-15s)
- **多对局检测** — 仅 `GameState.DebugPrintPower()` 的 `CREATE_GAME` 触发重置，避免双重复位
- **对手手牌情报4层架构**:
  - 确认手牌 (SHOW_ENTITY → HAND, X%)
  - 回手卡牌 (PLAY → HAND, 100%)
  - 入墓卡牌 (PLAY → GRAVEYARD)
  - 贝叶斯预测 (>50% 概率)
- **Config 集中化重写** — `analysis/config.py` 统一管理引擎参数
- **死代码清理** — 移除 `_player_entities`, `_in_full_entity`, `_in_show_entity`, `controller_map`, `_update_controller()` 等
- **诊断零错误** — 全部文件通过 IDE 诊断检查

### 2. 名称归一化
- **新增 `analysis/utils/player_name.py`**:
  - `normalize_player_name()` — 清洗空字符串/UNKNOWN/零宽字符/BOM
  - `is_anonymous_name()` — 检测匿名玩家 (UNKNOWN 开头)
  - `name_matches()` — BattleTag 匹配 + 大小写不敏感 + 后缀剥离
    - `"Player#1234"` 匹配 `"Player"`
    - `"Player#1234"` **不**匹配 `"Player#5678"`
- **GameReplayer 集成** — `_handle_game_debug_line()` 从 `DebugPrintGame()` 的 `PlayerName=` 自动检测玩家名
- **PacketReplayer 集成** — `_handle_create_game()` 使用 `normalize_player_name` + `is_anonymous_name` 替代 `startswith("UNKNOWN")`
- **replay_game.py** — `--player` 默认值改为空字符串（自动检测），移除硬编码 `"湫然#51704"`

### 3. Card 模型方法迁移
- **Card 新增方法** (`analysis/models/card.py`):
  - `has_mechanic(keyword)` — 快速 mechanics 检查
  - `get_effects()` — 委托给 `card_effects.get_effects()`
  - `compute_mechanics()` — 集成 `card_cleaner.extract_mechanics()`
  - `effective_overload()` / `effective_armor()` — 结构化字段 + 文本正则回退
  - `total_damage()` — direct + random damage 快捷访问
  - `is_minion` / `is_spell` / `is_weapon` / `is_hero` / `is_location` — 类型谓词
  - `mechanics_set` — 缓存的 set 属性
- **Minion 新增属性** (`analysis/search/game_state.py`):
  - `is_friendly` / `is_enemy` — 归属判断
  - `can_attack_now` — 综合攻击可用性 (冻结/风怒/休眠)
  - `total_stats` — attack + health 总和
- **调用方迁移** — `rhea_engine.py`, `strategic.py`, `hero_card_handler.py` 已改用 Card 实例方法

### 4. 新增模块
- `analysis/data/card_effects.py` — 结构化效果提取 (damage/aoe/heal/armor/draw/summon/buff/discard/cost_reduce)
- `analysis/constants/hs_enums.py` — 枚举定义
- `analysis/constants/__init__.py`

---

### 5. 核心领域模型抽象 (#28)
- **`models/card.py`** — 新增 `MinionData`/`SpellData` frozen dataclass，Card 添加可选 `minion_data`/`spell_data` 字段，`__post_init__` 自动同步保持向后兼容
- **`search/game_state.py`** — Minion 添加 `card_ref` 字段（反向引用源 Card）+ `Minion.from_card()` 工厂方法，自动从 Card mechanics 提取关键词
- **`search/rhea/simulation.py`** — `_play_minion()` 从 20 行手动字段映射重构为 `Minion.from_card(card)` 单行调用
- **`watcher/hero_card_handler.py`** — 修复遗漏的 `get_card_armor` import（原有 bug，被 bare except 吞掉）
- **设计决策**: 组合优于继承、保持 Card/Minion 分离、零破坏性迁移
- **测试**: 698 全通过

---

### 6. 回放系统修复 (3 个 bug)
- **`GameState.location_full()` 缺失** — 新增方法 `len(self.locations) >= 2`（炉石地标上限 2 个）
- **GameReplayer 双重处理** — `_process_line()` 仅接受 `GameState.DebugPrintPower()`，跳过 PowerTaskList 源，消除 FULL_ENTITY/SHOW_ENTITY/TAG_CHANGE 双重计数
- **GameReplayer FIRST_PLAYER bug** — 不再假设先手；`_we_are_first` 标志追踪先后手，修复后手时回合奇偶反转
- **PacketReplayer 跨局 decisions 清空** — 移除 `_process_game()` 中 `self.decisions.clear()`，决策跨对局累积
- **验证**: 698 测试通过，9 局日志 91 个决策点全部正确归属 `湫然#51704`

### 7. 卡牌名称数据库升级
- **`hsdb.py`** — 新增全量卡牌数据库加载（`cards.json`，34881 张 vs 原收藏 7898 张），覆盖衍生物、附魔、英雄技能等非收藏卡牌
- **`MUDAN_COIN1` → 幸运币**、`CATA_158t` → 希奈丝特拉的士兵 等全部解析成功
- **`packet_replayer.py`** — `_load_card_names()` 优先加载 `cards.json` 全量数据库
- **验证**: 日志中零原始卡牌 ID 残留

---

## ⏳ 未完成任务

暂无

---

## 🐛 已发现缺陷

### 缺陷 #1: bare except 吞异常泛滥 [中]
- **位置**: `rhea_engine.py` — 30+ 处 `except Exception: pass`
- **影响**: HeroCardHandler 失败走 fallback 但无日志；卡牌效果解析错误完全丢失；调试困难
- **建议**: 至少 `log.warning` 记录异常信息

### 缺陷 #2: 奥秘系统未完整 [中]
- **位置**: `analysis/search/secret_triggers.py`
- **待办**:
  1. 完整奥秘池按职业枚举 (Paladin, Hunter, Mage, Rogue)
  2. 多奥秘排序 (oldest first)
  3. Counter/Spellbane 交互
  4. L132: 未追踪最后死亡随从
- **影响**: 奥秘触发预测不完整

### ~~缺陷 #3: hero_card_handler 异常处理不完善~~ [已修复]
- **修复**: 补充 `get_card_armor` 缺失 import，原异常被 bare except 静默吞掉

### ~~缺陷 #4: PacketReplayer 与 GameReplayer 逻辑未对齐~~ [已解决]
- **解决**: 已删除 `game_replayer.py`（正则回放），统一使用 `packet_replayer.py`（hslog回放）
- **位置**: `packet_replayer.py`

---

## 📁 关键文件索引

| 文件 | 职责 |
|------|------|
| `analysis/config.py` | 集中配置 (引擎参数、数据路径) |
| `analysis/models/card.py` | Card dataclass + 解析方法 + 类型谓词 |
| `analysis/utils/player_name.py` | 玩家名归一化工具 |
| `analysis/data/card_effects.py` | 结构化效果提取 |
| `analysis/data/card_cleaner.py` | mechanics 正则提取 (50+ 模式) |
| `analysis/scorers/constants.py` | EFFECT_PATTERNS + CONDITION_DEFS |
| `analysis/scorers/scoring_engine.py` | 三层评分架构 (L2/L_struct/L3) |
| `analysis/search/rhea_engine.py` | V11 进化引擎核心 |
| `analysis/search/game_state.py` | Minion/HeroState/GameState |
| `analysis/watcher/packet_replayer.py` | hslog 包解析回放器 |
| `analysis/watcher/global_tracker.py` | 全局状态追踪 + 对手情报 |
| `scripts/replay_game.py` | 回放入口脚本 |
